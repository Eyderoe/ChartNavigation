版本号规范
1 加入大功能
2 加入小功能,修改代码
3 修改/备份代码


# 日本🔞AI未经审核代码
- 虽然未审核代码呈膨胀趋势, 但通过分文件, 勉强可控 [函数✅,测试❌]
- 先停一下, 不能加功能了, 再加代码不受控了要
回放数据
MapDataQuery[函数✅,测试❌]
geographic[函数✅,测试✅]
itemManage[函数❌,测试❌]
EnhancedMap
pdfView
main_widget
main_window


# 现有问题
vspeed也炸了(检查了下应该是vpilot的问题[xp内部显示也这样])
windows:
- MinGW内部错误, 造成调试时段错误
mac:
- 置顶窗口无效[其它应用最大化时]. 仅使用 Qt::WindowStaysOnTopHint 无法覆盖其它应用的最大化/全屏窗口；曾临时新增并已撤回 `src/ui/macWindow.hpp/.cpp/.mm`，通过 Objective-C++ 获取 Qt 的原生 `QNSView`，再取其 `NSWindow`，尝试设置 `NSStatusWindowLevel`、`NSWindowCollectionBehaviorCanJoinAllApplications`、`CanJoinAllSpaces`、`FullScreenAuxiliary` 和 `hidesOnDeactivate = NO`。最初错误地把 `QWindow::winId()` 直接转换为 `NSWindow*`，导致 `-[QNSView setLevel:]` 崩溃，后已改为 `reinterpret_cast<NSView *>(winId())->window`；修正后仍无法显示在其它应用上层，因此整个 macWindow 实现已回退。期间曾在 `build` 和 `cmake-build-debug` 编译验证，当前代码中已无 macWindow 相关文件和引用。后续研究重点：确认 macOS“最大化”是否实际为独立全屏 Space，检查窗口创建时机/Qt 是否重置原生属性，并考虑使用 `NSPanel` 或更高层级的独立 overlay 窗口；不要直接把 `winId()` 当作 `NSWindow*`。


# 近期改进
支持.Tnavi(可能的 其它文件名称显示),并自行计算页面角度.但这样变换质量和误差又没了，考虑新增字段 或者构造误差
进一步完善info-freq,最好都通改一遍,加入计算垂直速度。轨迹(正常1min安卓10min),地速(20s),航向/垂直(10s)
加入飞行器居中功能



# 远期改进
补函数注释
进一步增强debug能力(虽然现在现在回放配合调试已经很好了)
怪东西,有些时候某些字体 窗下面的囱->囪
可能的支持tabwidget以显示多张航图？
更合理的暗色逻辑,且必须在QPdfView下实现
可能的QSetting分组


