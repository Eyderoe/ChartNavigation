版本号规范
1 加入大功能
2 加入小功能,修改代码
3 修改/备份代码

# 现有问题

1.设置里的显示缩略图还没用
2.还是要卡住，哪里不对
3.好像是纯矢量渲染，后面有透明的
4.不知道亮色、暗色这一坨怎么样

XPlaneUDP,在Qt下析构还是会炸,需要进一步检查和确定(mac+xp11下不会)
XPlaneUDP,设置了TCAS那一堆以后,原来的info没有了.是XP不发了?还是处理哪里有问题(需要进一步验证与复现)
有时候没有更新(推断是没有绘制事件导致，可能的在连接时2Hz的重绘请求。不对，很怪 其实有 xpInfoUpdate 的)
xp连接航班号乱飞(复现请求)

暗色主题图标,在WIN下不可用

# 近期改进
文件缩略图(包括暗色逻辑)
更合理的暗色逻辑,且必须在QPdfView下实现
切换时,页面相对位置锁定
比例的缩放条，滑动条在main_widget，但缩放是在pdf_view中
更合适的多人游戏机膜信息显示(主要是描边)
一些其他页面，比如便签、单位换算等功能页面？不一定合适
支持包含地理数据的PDF，参考(avitab)好像用的mupdf还是啥
可能的仿射变换质量评估，2604昆明要一坨了

# 远期计划
不同机型映射不同，参考lnm或者fr24，或者哪找找免费的剪影
支持文件名称映射，或者进一步的支持单文件映射至多文件

# 吐槽
原来avitab都能做到，那写这勾子干嘛，有空测测，bilibili评论。chartFox好像真行,不知道为什么下载下来就不行了
