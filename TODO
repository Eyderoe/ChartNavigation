版本号规范
1 加入大功能
2 加入小功能,修改代码
3 修改/备份代码

# 现有问题
XPlaneUDP,设置了TCAS那一堆以后,原来的info没有了.是XP不发了?还是处理哪里有问题(需要进一步验证与复现)
有时候没有更新(推断是没有绘制事件导致，可能的在连接时2Hz的重绘请求。不对，很怪 其实有 xpInfoUpdate 的)
xp连接航班号乱飞(复现请求) 全程抓包好了 顶天两小时
坏完了 有时候局域网/XPlane 在线显示是完全乱的

# 近期改进
更合理的暗色逻辑,且必须在QPdfView下实现
缩放参考lnm！！！很重要
为什么zoomFactorChanged没用? 尽量解决闪屏问题
切换时,页面相对位置锁定(利用侧边栏)
支持包含地理数据的PDF，参考(avitab)好像用的mupdf还是啥
支持计算航向而不是使用导航数据(计算时一起把地速给算了)
显示信息参考空客ATSAW，不改动protobuf了，就这样挺好

# 远期计划
不同机型映射不同，参考lnm或者fr24，或者哪找找免费的剪影
支持文件名称映射，或者进一步的支持单文件映射至多文件
可能的PDF页面旋转
考虑在mac端使用plist原生格式而不是ini

# 吐槽
补函数注释
沟槽的copilot，直接让好多进度回档了，好久没看都不知道哪些可以用 哪些用不了
假如tls后面自己更好了，就通过zibo研究研究chartfox
